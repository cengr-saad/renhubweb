# Docker Compose configuration for LeasoRent Web Production
services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      # Database access URL (example format shown below)
      - DATABASE_URL=postgresql://postgres.mpjvjuhfrlybitvhsvvw:Formula12.13FORMULA@aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres
      # Supabase integration (if used by the client/server)
      - VITE_SUPABASE_URL=https://mpjvjuhfrlybitvhsvvw.supabase.co
      - VITE_SUPABASE_ANON_KEY=sb_publishable_beCqGLOQ3SDsrBKS_lAQmQ_PGJlwN8VEAS_NO_VCS=1
      - NODE_ENV=production
      - PORT=5000
    restart: always

# Instructions to build and run:
# 1. Ensure you have Docker and Docker Compose installed.
# 2. Update the 'environment' variables above with your actual values.
# 3. From this directory (web/), run:
#    docker-compose up -d --build
